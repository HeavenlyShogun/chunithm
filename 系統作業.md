幫您整理這份充滿熱血（與 AI 協助）的「**ESP32 Penguin Controller**」16 鍵中二節奏 (Chunithm) 控制器 DIY 專案指南！

這份文件詳細記錄了如何利用 ESP32-S3 與 MPR121 觸控模組，從零開始打造一台街機控制器。以下為您結構化統整的技術重點：

### 💻 軟體與韌體設定 (Software)

本專案分為兩個核心部分：**ESP32 韌體**（讀取硬體訊號）與 **chuniio.dll**（與遊戲本體溝通）。

* **ESP32 韌體編譯**：建議使用 VSCode 搭配 ESP-IDF 擴充套件，選擇您的開發板與 COM Port 後直接 Build & Flash。若不想處理環境設定，可以直接下載作者提供的 Release 包。
* **chuniio.dll 編譯**：使用 GCC 環境（如 MSYS2/MinGW）進行編譯，指令如下：
`gcc -shared -O2 -Wall -Wextra -std=c11 -static-libgcc -o .\chuniio\chuniio.dll .\chuniio\chuniio.c`
* **遊戲參數設定 (`segatools.ini`)**：將編譯好的 dll 放入指定路徑後，需修改設定檔，關鍵參數包含：
* `com_port`：填寫 ESP32 所在的 COM Port 數字。
* `baud_rate`：設定為 2000000。
* `air1_threshold` ~ `air6_threshold`：設定紅外線觸發數值（範圍 0~4095，建議進 Test Mode 實際測試調整）。



---

### 📦 硬體材料清單 (Hardware)

目前版本支援 **16 鍵**觸控輸入（若要擴充至 32 鍵需增加 MPR121 數量）。

| 元件名稱 | 數量 | 用途說明 |
| --- | --- | --- |
| **ESP32-S3** | 1 | 核心開發板 |
| **MPR121 觸控模組** | 2 | 處理觸控訊號 (32鍵需 3 個) |
| **紅外線 (IR) LED** | 6 | Air 感測器 (發射端) |
| **紅外線光電晶體** | 6 | Air 感測器 (接收端) |
| **1M 歐姆電阻** | 6 | 用於光電晶體的上拉電阻 |
| **33 歐姆電阻** | 2 | 用於 IR LED (限制電流) |
| **銅箔膠帶 / 紙箱** | 若干 | 製作觸控面板與機身外殼 |
| **各式線材** | 7-8 色 | 方便辨識各區塊接線 |

---

### 🛠️ 電路與組裝教學 (Assembly)

* **觸控面板製作**：
將銅箔膠帶貼在壓克力或紙板上（官方尺寸約 45cm x 10cm），切割成 16 個獨立不導電的區塊。每個區塊焊上連接線，表面可貼上霧面膠帶增加滑動手感。
* **MPR121 模組設定 (I2C 地址修改)**：
由於使用了兩個相同的模組，必須更改其中一個的 I2C 地址。請切斷模組背面的預設連線，並將 **ADD 腳位與 SDA 腳位短路**。兩片模組的 SDA 接至 ESP32 **GPIO 7**，SCL 接至 **GPIO 8**。
* **Air 感測器 (手部高度偵測)**：
將 IR LED 與光電晶體隔空對齊（距離約 3cm）。
* **接收端電路**：將 1~6 號光電晶體依序接至 ESP32 的 **GPIO 9~14**，務必搭配 **1M 歐姆電阻**，否則訊號會太弱導致長壓。
* **發射端電路**：5V 電源經過 **33 歐姆電阻**後連接至兩側的 IR LED。



---

### ⚠️ 已知問題與避坑指南 (Known Issues)

* **觸控失靈 / 幽靈觸控**：這通常是因為線材過長，產生了類似天線接收干擾的效應。建議將觸控面板到 MPR121 之間的**線材盡量縮短**。
* **部分觸控區塊無反應**：可能是 ESP32 供電不穩影響了 MPR121 的 I2C 暫存器。可以嘗試在 MPR121 的 VCC 與 GND 之間**增加電容**來穩壓。
* **終極除錯法**：如果以上都無效，請發揮 DIY 精神，**狂按 ESP32 上的 RST (Reset) 按鈕**直到它正常運作為止 XD。